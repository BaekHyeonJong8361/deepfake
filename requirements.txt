# ===================================================================
# 딥페이크 탐지 프로젝트 — 학습/평가/추론 재현용 의존성
# 환경: Python 3.9.21, NVIDIA RTX 4500 Ada (CUDA 12.8, cuDNN 9.10.2)
# 학과서버 .venv 기준으로 동결(freeze)한 실제 설치 버전
#
# [중요] 설치 순서
#   1) PyTorch는 CUDA 12.8 빌드를 별도 인덱스에서 설치:
#        pip install torch==2.8.0 torchvision==0.23.0 \
#            --index-url https://download.pytorch.org/whl/cu128
#   2) 나머지:  pip install -r requirements.txt
#
# [중요] cuDNN 워크어라운드 (학과서버 환경)
#   시스템 cuDNN과 PyTorch 번들 cuDNN 심볼 충돌로 RuntimeError 발생 시,
#   학습 스크립트 set_seed()에서  torch.backends.cudnn.enabled = False  설정.
#   (속도 약간 저하, 정확도 영향 없음)
# ===================================================================

# ----- 딥러닝 코어 (CUDA 12.8 빌드: 위 --index-url 로 설치 권장) -----
torch==2.8.0
torchvision==0.23.0

# ----- 모델 / 백본 -----
timm==1.0.26
transformers==4.57.6
huggingface-hub==0.36.2
safetensors==0.7.0
tokenizers==0.22.2

# ----- 주파수 도메인 (F3Net / FAD 브랜치) -----
torch-dct==0.1.6

# ----- 얼굴 검출 / 전처리 -----
mediapipe==0.10.9
opencv-python-headless==4.13.0.92
# facenet-pytorch  # preprocess.py 의 MTCNN 검출에 필요 (현재 venv 미설치, 전처리 재현 시 설치)

# ----- 데이터 / 수치 / 평가 -----
numpy==2.0.2
pandas==2.3.3
scikit-learn==1.6.1
scipy==1.13.1
Pillow==10.2.0

# ----- 추론 서버 (inference_server_v10) -----
fastapi==0.128.8
uvicorn==0.39.0
requests==2.32.5

# ----- 시각화 / 리포트 -----
matplotlib==3.9.4
python-pptx==1.0.2
lxml==6.1.0

# ----- SNS 데이터 수집 (crawl_sns_images) -----
icrawler==0.6.10

# ----- 유틸 -----
tqdm==4.67.3
pyyaml==6.0.3
regex==2026.1.15
filelock==3.19.1
